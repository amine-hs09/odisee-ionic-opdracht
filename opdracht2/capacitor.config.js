const config = {
  appId: 'be.aminehs.concertapp', // Ton Bundle ID
  appName: 'ConcertAppMohamedAmine', // Le nom de ton app
  webDir: 'dist',
};

// Utilisation ESM car `package.json` contient "type": "module"
export default config;