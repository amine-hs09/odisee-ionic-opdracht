// Pas d'import type ici
// const config: CapacitorConfig = { ... } devient juste :
const config = {
  appId: 'ionic.powerpoint1', // Garde l'appId que tu avais
  appName: 'powerpoint1',     // Garde l'appName que tu avais
  webDir: 'dist'
};

export default config;